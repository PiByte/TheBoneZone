// defines types, like 2d vectors, images and stuff of that nature
// will be used later when we decide to use custom formats for our game assets

class Vector
{
	constructor(x, y) { this.x = x; this.y = y; }
}